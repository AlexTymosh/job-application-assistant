# Optional app data override. The normal product flow is the Data Folder page.
APP_DATA_DIR=./.local-data

# Optional process-level fallback for local development only.
# The application settings UI stores the OpenAI API key through OS keyring instead.
OPENAI_API_KEY=
